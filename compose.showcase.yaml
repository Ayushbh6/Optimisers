services:
  supplier-basket-showcase:
    build:
      context: .
      dockerfile: Dockerfile.showcase
    ports:
      - "8765:8765"
    restart: "no"
    read_only: true
    tmpfs:
      - /tmp:size=16m
    security_opt:
      - no-new-privileges:true
