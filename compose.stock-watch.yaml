services:
  stock-watch-showcase:
    build:
      context: .
      dockerfile: Dockerfile.stock-watch
    ports:
      - "8767:8766"
    restart: "no"
    read_only: true
    tmpfs:
      - /tmp:size=16m
    security_opt:
      - no-new-privileges:true
